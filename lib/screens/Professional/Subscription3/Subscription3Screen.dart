import 'package:flutter/material.dart';

class Subscription3Screen extends StatelessWidget {
  const Subscription3Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Subscription 3'),
      ),
      body: const Center(
        child: Text('Subscription 3 Screen'),
      ),
    );
  }
}

