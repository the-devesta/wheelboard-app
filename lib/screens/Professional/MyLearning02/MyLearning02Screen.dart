import 'package:flutter/material.dart';

class MyLearning02Screen extends StatelessWidget {
  const MyLearning02Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('My Learning 02'),
      ),
      body: const Center(
        child: Text('My Learning 02 Screen'),
      ),
    );
  }
}

