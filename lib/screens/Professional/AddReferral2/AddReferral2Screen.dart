import 'package:flutter/material.dart';

class AddReferral2Screen extends StatelessWidget {
  const AddReferral2Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Add Referral 2'),
      ),
      body: const Center(
        child: Text('Add Referral 2 Screen'),
      ),
    );
  }
}

