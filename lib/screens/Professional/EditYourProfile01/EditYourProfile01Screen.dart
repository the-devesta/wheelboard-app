import 'package:flutter/material.dart';

class EditYourProfile01Screen extends StatelessWidget {
  const EditYourProfile01Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Edit Your Profile 01'),
      ),
      body: const Center(
        child: Text('Edit Your Profile 01 Screen'),
      ),
    );
  }
}

