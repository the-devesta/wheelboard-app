import 'package:flutter/material.dart';

class CalendarMarkDateScreen extends StatelessWidget {
  const CalendarMarkDateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Calendar Mark Date'),
      ),
      body: const Center(
        child: Text('Calendar Mark Date Screen'),
      ),
    );
  }
}

